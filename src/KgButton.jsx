function KgButton() {
  return <button>
    Like This
  </button>
}

export default KgButton;

// can use also as export default function KgButton. but this written is best. 

// can also export multiple functions from one file 
// using export before fucntion and then you have to use import {KgButton} for desired button you want to import.
// defining components : the naming convention is that is should be camel casing with first word as Capital. 